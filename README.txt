========================Enrollement System============================

1. In the project directory, run the bundle install
2. run rake db:migrate
3. run rake db:seed
4. start the rails server
5. in the browser go the the url localhost:3000/courses to start using the application